<div class="flex flex-end nav">
        {{#foreach navigation}}
            <div class="flex-auto nav-box nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></div>
        {{/foreach}}
</div>
